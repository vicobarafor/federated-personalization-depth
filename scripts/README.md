# Scripts

- `aggregate_results.py`: creates a compact benchmark summary from stored result files.
- `reproduce_figures.py`: verifies the final README figures are present.

The full training sweep is computationally expensive; stored outputs are provided for inspection and analysis.
